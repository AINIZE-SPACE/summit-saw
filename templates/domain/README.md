# Replace With Domain Name

This domain contains programmatic memory for `replace-with-domain`.

Recommended branch:

```text
domain/replace-with-domain
```

## Purpose

- ...

## Source Projects

- ...

## Boundaries

Do not promote:

- secrets or credential paths;
- current external service state without re-verification;
- project-only paths that are not portable;
- unapproved discussion-only plans.

## Promotion Path

```text
agent/<agent>/replace-with-domain/<topic>
  -> domain/replace-with-domain
  -> ascent
  -> main
```
