# Harness Agent Skills

Harness adapters belong here when a base skill needs Harness-specific behavior.

Examples:

- pipeline execution constraints;
- deployment environment metadata;
- approval gates;
- CI/CD artifact handling.

Keep reusable workflow logic in `skills/base`.
