# Claude Code Agent Skills

Claude Code adapters belong here when a base skill needs Claude Code-specific behavior.

Examples:

- command conventions;
- project memory files;
- slash-command or hook behavior;
- local install layout.

Keep reusable workflow logic in `skills/base`.
