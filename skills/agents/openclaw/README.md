# OpenClaw Agent Skills

OpenClaw adapters belong here when a base skill needs OpenClaw-specific behavior.

Examples:

- plugin bridge behavior;
- command/runtime conventions;
- local skill installation layout;
- model or tool compatibility notes.

Keep reusable workflow logic in `skills/base`.
