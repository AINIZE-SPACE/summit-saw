# Codex Agent Skills

Codex adapters belong here when a base skill needs Codex-specific behavior.

Examples:

- final response directives;
- project/global skill paths;
- memory citation requirements;
- available tool constraints;
- Codex plugin or connector behavior.

Keep reusable workflow logic in `skills/base`.
